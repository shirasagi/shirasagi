<%= form_for :item, url: { action: :import }, html: { id: "item-form", method: :post, multipart: true, autocomplete: :off } do |f| %>
  <%= error_messages_for :item %>

  <div class="addon-views">
    <div class="addon-view" id="addon-basic">
      <header><h2><%= t("ldap.views.confirm.import") %></h2></header>
      <p><%= t "ldap.views.confirm.import_description" %></p>
    </div>
  </div>

  <footer class="send">
    <%= f.submit t("ldap.views.button.import"), class: :save %>
    <%= f.button t("views.button.cancel"), onclick: "location.href='#{url_for(action: :index)}'; return false;" %>
  </footer>
<% end %>
