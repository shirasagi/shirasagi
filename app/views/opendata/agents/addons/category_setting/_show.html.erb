<%
  st_categories = @item.st_categories.presence || @item.default_st_categories
%>
<dl class="see">
  <dd class="wide"><%=br st_categories.map(&:name) %></dd>
</dl>

<% if @item.categories_limit.to_i > 0 %>
  <dl class="see">
    <dt><%= @item.t :categories_limit %></dt>
    <dd><%= @item.categories_limit %></dd>
  </dl>
<% end %>
