<%

categories = Opendata::DatasetGroup.site(@cur_site).sort(order: 1)

def cate_form(item)
  h  = %Q[<label style="margin-right: 10px;">]
  h << f.check_box("dataset_group_ids[]", {}, item._id)
  h << %Q[#{item.name}</label>]

  h.html_safe
end

%>
<dl class="see mod-opendata-dataset_group">
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "dataset_group_ids[]", value: "" %>

    <% categories.each do |cate| %>
    <span style="inline-block"><%= cate_form cate %></span>
    <% end %>
  </dd>
</dl>
