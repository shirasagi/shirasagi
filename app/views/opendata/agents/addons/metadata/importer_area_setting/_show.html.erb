<%
  return if @item.default_areas.blank?

  addon ||= local_assigns.fetch(:addon, {})
  addon[:display_body] = "hide"
%>

<dl class="see mod-opendata_metadata_area_setting">
  <dt><%= @model.t :default_area_ids %></dt>
  <dd><%= @item.default_areas.pluck(:name).join(", ") %></dd>
</dl>
