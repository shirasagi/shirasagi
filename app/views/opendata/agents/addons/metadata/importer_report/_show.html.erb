<% return "" if @item.reports.blank? %>

<dl class="see mod-opendata-dataset_import-report">
  <dd class="wide">
    <table class="index">
      <% @item.reports.order(created: -1).limit(5).each do |report| %>
        <tr>
          <td>
            <%= link_to opendata_metadata_importer_report_path(importer_id: @item.id, id: report.id) do %>
              <%= ss_time_tag report.created %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </table>
  </dd>
</dl>
