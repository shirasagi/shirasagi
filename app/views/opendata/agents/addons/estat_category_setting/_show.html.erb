<%
  st_estat_categories = @item.st_estat_categories.presence || @item.default_st_estat_categories
%>
<dl class="see">
  <dd class="wide"><%=br st_estat_categories.map(&:name) %></dd>
</dl>

<% if @item.estat_categories_limit.to_i > 0 %>
  <dl class="see">
    <dt><%= @item.t :estat_categories_limit %></dt>
    <dd><%= @item.estat_categories_limit %></dd>
  </dl>
<% end %>
