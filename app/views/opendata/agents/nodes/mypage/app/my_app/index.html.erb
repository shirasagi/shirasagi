<nav class="menu">
  <a href="new/">新規作成</a>
</nav>
<nav class="tutorial-data">
	<a class="tutorial-app" href="/tutorial-app.html">アプリ登録手順</a>
</nav>
<table class="opendata-apps index">
  <thead>
    <tr>
      <th class="name">アプリ名</th>
      <th class="updated">更新日時</th>
      <th class="state">状態</th>
      <th class="point">いいね</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td><a href="<%= "#{item.id}/" %>"><%= item.name %></a></td>
      <td><%= tryb{item.updated.strftime("%Y年%1m月%1d日 %1H時%1M分")} %></td>
      <td><%= item.label :state %></td>
      <td><%= item.point.to_i %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @items if @items.try(:current_page) %>
