<%= render file: "_search" %>

<table class="index">
  <thead>
    <tr>
      <th class="checkbox"><input type="checkbox" /></th>
      <th><%= @model.t :name %></th>
      <th class="original_url"><%= @model.t :original_url %></th>
      <th class="datetime"><%= @model.t :updated %></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
    <tr>
      <td>
        <input type="checkbox" name="ids[]" value="<%= item.id %>" />
        <nav class="tap-menu">
          <%= link_to :show, { action: :show, id: item } %>
          <%= link_to :edit, { action: :edit, id: item } %>
          <%= link_to :delete, { action: :delete, id: item } %>
        </nav>
      </td>
      <td><%= link_to item.name, { action: :show, id: item } %></td>
      <td class="original_url"><%= item.original_url %></td>
      <td class="original_updated"><%= item.original_updated.strftime("%Y/%m/%d %H:%M") %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @items if @items.try(:current_page) %>
