<dl class="see">
  <dt><%= @model.t :name %></dt>
  <dd><%= @item.send :name %></dd>

  <dt><%= @model.t :basename %></dt>
  <dd><%= @item.send :basename %></dd>
  <dd>
    <%= @item.public? ? link_to(@item.full_url, @item.full_url, target: "_blank", rel: "noopener") : @item.full_url %>
    (
    <% if @cur_site.mobile_enabled? %>
      <%= link_to t("ss.links.pc_preview"), cms_preview_path(path: @item.preview_path), target: "_blank", rel: "noopener" %>
      |
      <%= link_to t("ss.links.mobile_preview"), cms_preview_path(path: @item.mobile_preview_path), target: "_blank", rel: "noopener" %>
    <% else %>
      <%= link_to t("cms.preview_page"), cms_preview_path(path: @item.preview_path), target: "_blank", rel: "noopener" %>
    <% end %>
    )
  </dd>

  <dt><%= @model.t :text %></dt>
  <dd><%= auto_link br(@item.text), html: { target: "_blank", rel: "noopener" } %></dd>

  <dt><%= @model.t :license %></dt>
  <dd><%= @item.send :license %></dd>

  <dt><%= @model.t :tags %></dt>
  <dd><%= @item.send :tags %></dd>

  <dt><%= @model.t :appurl %></dt>
  <dd><%= auto_link br(@item.appurl), html: { target: "_blank", rel: "noopener" } %></dd>

  <dt><%= @model.t :point %></dt>
  <dd><%= @item.point %></dd>
</dl>
