<div class="addon-views">
  <div class="addon-view" id="addon-basic">
    <div class="addon-head"><h2><%= t("sns.file_gen_task.failed") %></h2></div>
    <div class="addon-body">
      <p>The process of creating the download data completed successfully, but the download data was not generated.</p>
      <p>An error or other issue may have occurred while creating the data for download.</p>
      <p>Please check the job log for details, such as the cause of the error.</p>
      <p class="mt-2"><%= link_to t("mongoid.models.job/log"), job_sns_logs_path, data: { turbo: false } %></p>
    </div>
  </div>
</div>
