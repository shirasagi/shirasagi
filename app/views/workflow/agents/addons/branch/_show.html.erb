<%
  return unless @item.allowed?(:edit, @cur_user, site: @cur_site, node: @cur_node)
  return unless @item.persisted?
%>

<turbo-frame id="workflow-branch-frame" src="<%= workflow_frames_branch_path(id: @item) %>">
  <%= loading %>
</turbo-frame>
