<dl class="see">
  <dt><%= @model.t :webmail_role_ids %></dt>
  <dd>
    <% @item.webmail_roles.order_by(name: 1).each do |role| %>
      <div><%= link_to role.name, webmail_role_path(id: role) %></div>
    <% end %>
  </dd>
</dl>
