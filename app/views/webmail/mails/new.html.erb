<% @crud_buttons = proc do |f| %>
  <%= f.submit t("views.buttons.send"), class: 'btn-primary save js-send' %>
  <%= f.submit t("views.buttons.draft_save"), class: 'btn-default js-save' %>
<% end %>

<%= render file: "ss/crud/new" %>
