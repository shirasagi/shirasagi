<% @crud_buttons = proc do |f| %>
  <%= f.submit t("views.button.save"), class: 'btn-primary save' %>
  <%= f.submit t("views.button.save"), class: 'btn-default' %>
<% end %>

<%= render file: "ss/crud/edit" %>
