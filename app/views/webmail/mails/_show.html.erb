<%= jquery do %>
  Webmail_Mail_Detail.render();
<% end %>

<% if @item.html? %>
  <div class="body body--html"><%= @item.sanitize_html %></div>
<% else %>
  <div class="body body--text"><%= email_addresses_to_link auto_link(br(@item.text), :urls) %></div>
<% end %>
