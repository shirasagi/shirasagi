<% message.subject = "開封済み：#{@item.subject}" %>
次のユーザーに送信されたメッセージの開封確認です。
<%= @item.imap.email_address %> : <%= @item.internal_date.strftime("%Y/%m/%d %H:%M")%>

メッセージが開封されました。: <%= Time.zone.now.strftime("%Y/%m/%d %H:%M") %>
