<nav class="nav-menu">
  <% if params[:action] =~ /index/ %>
    <%= link_to t('ss.links.download'), action: :download_all %>
    <%= link_to t('ss.links.delete'), action: :batch_destroy %>
  <% else %>
    <%= link_to t('ss.links.back_to_index'), action: :index %>
  <% end %>
</nav>
