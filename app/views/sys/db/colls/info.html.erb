<div class="cms-tabs">
  <% path = url_for(action: :index) %>
  <%= link_to(path, class: [ params[:action] == "index" ? 'current' : nil ]) do %>
    <span class="tab-name">Collections</span>
  <% end %>
  <% path = url_for(action: :info) %>
  <%= link_to(path, class: [ params[:action] != "index" ? 'current' : nil ]) do %>
    <span class="tab-name">Server</span>
  <% end %>
</div>

<div class="addon-views">
  <%= render "ss/crud/addon", addon: { id: "addon-basic", head: "Server" } do %>
    <dl class="see">
      <dt>name</dt>
      <dd><%= db.database.name %></dd>
    </dl>
  <% end %>

  <%= render "ss/crud/addon", addon: { id: "addon-stats", head: "Hello" } do %>
    <%= link_to "https://docs.mongodb.com/manual/reference/command/hello/#mongodb-dbcommand-dbcmd.hello", target: "_blank", rel: "noopener" %>
    <%=
      begin
        db.command(hello: 1).documents.first.try do |result|
          content_tag(:dl, class: "see") { render "sys/db/colls/bson_document", result: result, depth: 1 }
        end
      rescue => e
        e.to_s
      end
    %>
  <% end %>

  <%= render "ss/crud/addon", addon: { id: "addon-stats", head: "Stats" } do %>
    <%= link_to "https://docs.mongodb.com/manual/reference/command/dbStats/#mongodb-dbcommand-dbcmd.dbStats", target: "_blank", rel: "noopener" %>
    <%=
      begin
        db.command(dbStats: 1).documents.first.try do |result|
          content_tag(:dl, class: "see") { render "sys/db/colls/bson_document", result: result, depth: 1 }
        end
      rescue => e
        e.to_s
      end
    %>
  <% end %>

  <%= render "ss/crud/addon", addon: { id: "addon-build-info", head: "Build Info" } do %>
    <%= link_to "https://docs.mongodb.com/manual/reference/command/buildInfo/#mongodb-dbcommand-dbcmd.buildInfo", target: "_blank", rel: "noopener" %>
    <%=
      begin
        db.command(buildInfo: 1).documents.first.try do |result|
          content_tag(:dl, class: "see") { render "sys/db/colls/bson_document", result: result, depth: 1 }
        end
      rescue => e
        e.to_s
      end
    %>
  <% end %>

  <%= render "ss/crud/addon", addon: { id: "addon-build-info", head: "Host" } do %>
    <%= link_to "https://docs.mongodb.com/manual/reference/command/hostInfo/#mongodb-dbcommand-dbcmd.hostInfo", target: "_blank", rel: "noopener" %>
    <%=
      begin
        db.command(hostInfo: 1).documents.first.try do |result|
          content_tag(:dl, class: "see") { render "sys/db/colls/bson_document", result: result, depth: 1 }
        end
      rescue => e
        e.to_s
      end
    %>
  <% end %>

  <%= render "ss/crud/addon", addon: { id: "addon-build-info", head: "Server Status" } do %>
    <%= link_to "https://docs.mongodb.com/manual/reference/command/serverStatus/#mongodb-dbcommand-dbcmd.serverStatus", target: "_blank", rel: "noopener" %>
    <%=
      begin
        db.command(serverStatus: 1).documents.first.try do |result|
          content_tag(:dl, class: "see") { render "sys/db/colls/bson_document", result: result, depth: 1 }
        end
      rescue => e
        e.to_s
      end
    %>
  <% end %>
</div>
