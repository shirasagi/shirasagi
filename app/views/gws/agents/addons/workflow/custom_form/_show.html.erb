<% return if @item.custom_values.blank? %>

<dl class="see mod-gws-workflow-custom_form">
<% @item.custom_values.each do |key, val| %>
  <dt><%= val['name'] %></dt>
  <dd><%= br [ Gws::Workflow::Column.from_mongo(val['input_type'], val['value']) ].flatten.join(', ') %></dd>
<% end %>
</dl>
