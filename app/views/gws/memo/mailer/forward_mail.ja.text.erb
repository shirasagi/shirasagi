メッセージが届きました。

件名: <%= @item.name %>
<%= @item.text %>
時刻: <%= @item.send_date.strftime("%Y/%m/%d %H:%M") rescue nil %>
送信者: <%= "#{@cur_user.name} <#{@cur_user.email}>" %>
受信者: <%= @cc_memo %>
<% if @item.files.present? %>
添付ファイル: <%= @item.files.pluck(:name).join(", ") %>
<% end -%>
