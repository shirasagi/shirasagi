<%= form_for :item, url: { action: :export }, html: { id: "item-form", method: :put, multipart: true } do |f| %>
  <%= error_messages_for :item %>

  <div class="addon-views">
    <div class="addon-view mb-5" id="addon-basic">
      <h2 class="h4"><%= t("ss.export") %></h2>
      <div class="addon-body">
        <div><%=br t("gws/memo/message.export.start_message") %></div>
      </div>
      <footer class="send">
        <%= link_to t('ss.links.back'), { action: :index } , { class: "btn btn-outline-primary" } %>
      </footer>
    </div>
  </div>
<% end %>
