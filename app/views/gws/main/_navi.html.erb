<nav class="main-navi">
  <!--
  <h2><%= link_to t('gws.portal'), "#{gws_portal_path}/", class: "icon-portal" %></h2>
  -->
  <h2><%= link_to t('modules.gws/schedule'), gws_schedule_plans_path, class: "icon-schedule" %></h2>
  <h2><%= link_to t('modules.gws/board'), gws_board_topics_path, class: "icon-board" %></h2>
  <h2><%= link_to t('modules.gws/workflow'), gws_workflow_files_path, class: "icon-content" %></h2>
  <h2><%= link_to t('modules.gws/share'), gws_share_files_path, class: "icon-conv" %></h2>
  <h2><%= link_to t('gws.site_config'), gws_site_path, class: "icon-conf" %></h2>
</nav>

<% links = Gws::Link.site(@cur_site).and_public.allow(:read, @cur_user, site: @cur_site) %>
<% if links.present? %>
<nav class="mod-navi">
  <h2><%= link_to t('mongoid.models.gws/link'), gws_public_links_path %></h2>
  <% links.each do |item| %>
  <h3><%= link_to item.name, gws_public_link_path(id: item) %></h3>
  <% end %>
</nav>
<% end %>
