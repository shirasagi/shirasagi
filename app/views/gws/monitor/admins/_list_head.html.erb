<%= jquery do %>
  Gws_Monitor_Topic.render();
<% end %>

<div class="list-head">
  <label class="check"><input type="checkbox" /></label>

  <div class="list-head-action">
    <button class="disable-all btn" data-ss-href="<%= url_for(action: :disable_all) %>" data-ss-confirm="<%= I18n.t('gws/monitor.confirm.disable_all') %>"><%= t 'gws/monitor.links.disable_all' %></button>
  </div>

  <%= render file: '_search' %>
</div>

