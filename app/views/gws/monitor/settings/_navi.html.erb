<nav class="mod-navi">
  <h2><%= t "mongoid.models.gws/monitor/group_setting" %></h2>

  <% if @cur_site.allowed?(:read, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('gws.module_setting'), gws_monitor_setting_path %></h3>
  <% end %>
  <% if Gws::Monitor::Category.allowed?(:read, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('mongoid.models.gws/monitor/group_setting/category'), gws_monitor_categories_path %></h3>
  <% end %>
</nav>

<%= render partial: "gws/main/conf_navi" %>

