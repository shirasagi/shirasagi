<%
#return render inline: "User Not Found."
model = Gws::User
item  = @item.user
%>

<dl class="see">
  <dt><%= model.t :name %></dt>
  <dd><%= @item.send :name %></dd>

  <dt><%= model.t :uid %></dt>
  <dd><%= @item.uid %></dd>

  <dt><%= model.t :email %></dt>
  <dd><%= @item.send :email %></dd>

  <% if @item.tel.present? %>
  <dt><%= model.t :tel %></dt>
  <dd><%= @item.send :tel %></dd>
  <% end %>

  <% if @item.user %>
  <dt><%= model.t :group_ids %></dt>
  <dd><%= br item.groups.order_by(name: 1).select{ |c| Gws::Group.site(@cur_site).pluck(:id).include?(c.id) }.map{|c| c.name}.join("\n") %></dd>
  <% end %>

  <dt><%= @model.t :order %></dt>
  <dd><%= @item.send :order %></dd>
</dl>
