<div class="list-head">
  <div class="operations">
    <%= link_to(t("ss.buttons.csv"), { action: :download_all, s: @s.to_h }, { class: "btn btn-csv", style: "font-weight: normal;" }) %>
  </div>

  <%= render file: "_search" %>
</div>
