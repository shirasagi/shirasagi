<dl class="see">
  <% if @item.image? %>
  <dt><%= @model.t :image %></dt>
  <dd><%= link_to image_tag(url_for(action: :thumb, _: @item.updated.to_i), alt: ""), { action: :view }, { target: "_blank", rel: "noopener", class: SS::DownloadPolicy.html_class } %></dd>
  <% end %>

  <dt><%= @model.t :filename %></dt>
  <dd><%= @item.send :name %><%= sanitizer_status(@item) %></dd>

  <% if SS::DownloadPolicy.download_allowed? %>
    <dt><%= @model.t :download %></dt>
    <dd><%= link_to t('ss.links.download'), { action: :download }, { class: "btn" } %></dd>
  <% end %>
</dl>
