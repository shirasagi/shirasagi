<div class="list-head">
  <div class="operations">
    <% if params[:action] == 'index' %>
      <%= button_tag(t("ss.buttons.csv"), name: "csv", type: "button", class: "btn btn-csv") %>
    <% end %>
    <%= button_tag(t("ss.buttons.print"), name: "print", type: "button", class: "btn btn-print") %>
  </div>

  <%= render file: "_search" %>
</div>
