<nav class="mod-navi current-navi">
  <h2><%= link_to t('modules.gws/questionnaire'), gws_questionnaire_main_path, class: 'icon-notice' %></h2>

  <h3><%= link_to t('ss.navi.readable'), gws_questionnaire_readables_path(folder_id: '-', category_id: '-'), class: 'readable' %></h3>

  <% if Gws::Questionnaire::Form.allowed?(:edit, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('ss.navi.editable'), gws_questionnaire_editables_path(folder_id: '-', category_id: '-'), class: 'management' %></h3>
  <% end %>

  <% if Gws::Questionnaire::Form.allowed?(:trash, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('ss.navi.trash'), gws_questionnaire_trashes_path, class: 'trash' %></h3>
  <% end %>

  <% if Gws::Questionnaire::Category.allowed?(:read, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('gws.category'), gws_questionnaire_categories_path, class: 'management' %></h3>
  <% end %>
</nav>

<%= render partial: 'gws/main/navi' %>
