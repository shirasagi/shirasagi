<nav class="nav-menu">
  <% if params[:action] =~ /edit|update|delete/ %>
    <%= link_to t('ss.links.back_to_show'), action: :show %>
  <% else %>
    <%=
      if @cur_user.gws_role_permit_any?(@cur_site, :edit_gws_user_profile)
        link_to t('ss.links.edit'), action: :edit
      end
    %>
    <%=
      if @cur_user.gws_role_permit_any?(@cur_site, :edit_password_gws_user_profile) && !@cur_user.type_sso?
        link_to t('ss.links.edit_password'), action: :edit_password
      end
    %>
  <% end %>
</nav>
