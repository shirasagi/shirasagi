<%

opts = {}

if params[:s][:start].present? || params[:s][:end].present?
  opts[:holiday] = [params[:s][:start].to_date, params[:s][:end].to_date]
end

%><%== calendar_format(@items, opts).to_json %>
