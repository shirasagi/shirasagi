<% @index_meta = proc do |item| %>
    <span class="datetime"><%= item.updated.strftime('%Y/%m/%d %H:%M') %></span>
    <span><%= item.todo_state_name %></span>
<% end %>

<%= render file: 'gws/crud/index' %>