<%= render file: 'app/views/gws/facility/custom_fields/input_forms/_common_label', locals: { item: item } %>
<dd>
  <% item.select_options.each do |val| %>
  <label>
    <%= radio_button_tag("#{object_name}[#{item.id}]", val, false, item.html_options) %>
    <%= val %>
  </label>
  <% end %>
</dd>
