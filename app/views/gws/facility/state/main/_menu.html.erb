<nav class="nav-menu">
  <% if SS::DownloadPolicy.download_allowed? %>
    <%= link_to(t('ss.links.download'), url_for(action: :download, s: params.to_unsafe_h[:s]), method: :post) %>
  <% end %>
</nav>
