お知らせに「<%= @item.name %>」が掲載されました。
<%= gws_notice_readable_url(protocol: @item.site.canonical_scheme, host: @item.site.canonical_domain, site: @item.site, folder_id: '-', category_id: '-', id: @item) %> をブラウザで開き、内容をご確認ください。
