<nav class="nav-menu">
  <% if params[:action] =~ /index|reply/ %>
    <%= link_to t('ss.links.back'), portal_path %>
    <% if @topic.allowed?(:edit, @cur_user, site: @cur_site) %>
      <%= link_to t("gws/discussion.links.topic.edit"), edit_gws_discussion_forum_thread_topic_path(id: @topic) %>
    <% end %>
    <% if @topic.allowed?(:edit, @cur_user, site: @cur_site) %>
      <%= link_to t("gws/discussion.links.topic.copy"), copy_gws_discussion_forum_thread_topic_path(id: @topic) %>
    <% end %>
    <% if @topic.allowed?(:delete, @cur_user, site: @cur_site) %>
      <%= link_to t("gws/discussion.links.topic.delete"), delete_gws_discussion_forum_thread_topic_path(id: @topic) %>
    <% end %>
  <% elsif params[:action] =~ /edit|update|delete/ %>
    <%= link_to(t('ss.links.back'), { action: :index }, class: "back-to-index") %>
  <% else %>
    <% if @item.allowed?(:delete, @cur_user, site: @cur_site) %>
      <%= link_to t('ss.links.delete'), { action: :delete, id: @item } %>
    <% end %>
  <% end %>
 </nav>
