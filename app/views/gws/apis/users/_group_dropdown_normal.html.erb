<% @groups ||= @cur_site.descendants_and_self.active.tree_sort(root_name: @cur_site.name) %>
<% options = [["(#{@cur_site.name})", @cur_site.id]] + @groups.to_options %>
<%= select_tag "s[group]", options_for_select(options, @group.try(:id)) %>
