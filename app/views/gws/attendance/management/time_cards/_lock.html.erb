<dl class="see">
  <dt class="wide"></dt>
  <dd class="wide"><%= l(@cur_month.to_date, format: :attendance_year_month) %>のタイムカード</dd>
  <dd class="wide">
    <% @target_users.each do |user| %>
      <label><%= check_box_tag "#{f.object_name}[user_ids][]", user.id, true, { id: nil } %><%= gws_public_user_long_name(user.long_name) %></label>
    <% end %>
  </dd>
</dl>
