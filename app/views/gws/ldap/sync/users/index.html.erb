<%
  return if dry_run_users.count.zero?
%>

<div class="main-box">
  <table class="index">
    <thead>
    <tr>
      <th><%= Gws::User.t :name %></th>
    </tr>
    </thead>
    <tbody>
    <% dry_run_users.each do |group| %>
      <tr>
        <td><%= link_to group.name, gws_ldap_sync_user_path(id: group.attributes["basename"]) %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
