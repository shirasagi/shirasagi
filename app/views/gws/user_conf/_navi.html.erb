<nav class="mod-navi">
  <h2><%= t('mongoid.models.ss/user') %></h2>

  <% if Gws::User.allowed?(:read, @cur_user, site: @cur_site) %>
    <h3><%= link_to t('mongoid.models.ss/user'), gws_users_path %></h3>
  <% end %>
  <% if Gws::UserForm.allowed?(:read, @cur_user, site: @cur_site) %>
    <h3><%= link_to Gws::UserForm.model_name.human, gws_user_form_path %></h3>
  <% end %>
</nav>

<%= render partial: 'gws/main/conf_navi' %>
