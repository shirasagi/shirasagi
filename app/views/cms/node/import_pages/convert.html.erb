<%= form_for :item, url: { action: :convert }, html: { method: :put, multipart: true } do |f| %>
<%= error_messages_for :item %>

<section class="main-box">
  <header><h2><%= t("ss.confirm.convert") %></h2></header>

  <%= f.hidden_field :route, value: "cms/page" %>
</section>

<footer class="send">
  <%= f.submit t("ss.buttons.convert"), class: :save %>
  <%= f.button t("ss.buttons.cancel"), type: :reset, onclick: "location.href='#{url_for(action: :show)}'; return false;" %>
</footer>
<% end %>
