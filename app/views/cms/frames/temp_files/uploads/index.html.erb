<div class="cms-tabs">
  <%= link_to tag.span(t("ss.buttons.upload"), class: "tab-name"), cms_frames_temp_files_uploads_path(accepts: accepts, s: params[:s].try(:to_unsafe_h)), class: "current", data: { turbo: true } %>
  <%= link_to tag.span(t("ss.buttons.select_from_list"), class: "tab-name"), cms_frames_temp_files_files_path(accepts: accepts, s: params[:s].try(:to_unsafe_h)), data: { turbo: true } %>
</div>

<%= render "sns/frames/temp_files/uploads/index" %>
