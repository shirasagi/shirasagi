<turbo-frame id="cms-nodes-tree-frame">
  <%= render(Cms::NodesTreeComponent.new(site: @cur_site, user: @cur_user, item: @item, only_children: params[:only_children], root_items: params[:root_items], type: params[:type])) %>
</turbo-frame>