<nav class="nav-menu">
  <%= link_to t("cms.links.back_to_contents"), cms_contents_path %>
  <% if params[:action] !~ /index/ %>
    <%= link_to t('ss.links.back_to_index'), action: :index %>
  <% end %>
</nav>
