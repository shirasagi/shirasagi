<dl class="see">
  <dt><%= @model.t :name %></dt>
  <dd><%= @item.send :name %></dd>

  <dt><%= @model.t :email %></dt>
  <dd><%= @item.send :email %></dd>

  <dt><%= @model.t :group_ids %></dt>
  <dd><%=br @item.groups.order_by(name: 1).select{ |c| Cms::Group.site(@cur_site).pluck(:id).include?(c.id) }.map{|c| c.name}.join("\n") %></dd>

</dl>
