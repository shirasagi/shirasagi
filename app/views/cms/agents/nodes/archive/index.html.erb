<div class="cms-pages pages">
  <% if @range_type == "yearly" %>
    <%= render template: "_yearly_list" %>
  <% else %>
    <%= render template: "_monthly_list" %>
  <% end %>
</div>

<%= paginate @items if @items.try(:current_page) %>
