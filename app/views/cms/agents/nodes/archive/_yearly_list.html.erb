<div class="cms-pages pages">
  <nav class="event-date">
    <h2 class="<%= @year %>"><%= "#{@year}#{t_date('year')}" %></h2>
  </nav>
  <%= render_page_list %>
</div>

<%= paginate @items if @items.try(:current_page) %>
