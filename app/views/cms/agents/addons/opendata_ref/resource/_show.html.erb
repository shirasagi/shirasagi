<% return "" if @cur_node.blank? %>
<% return "" if @cur_node.becomes_with_route.try(:opendata_site_ids).blank? %>
<% return "" if @item.opendata_dataset_state == 'none' %>
<% return "" if @item.attached_files.blank? %>

<% if @item.allowed?(:edit, @cur_user) %>
  <%= render 'cms/agents/addons/opendata_ref/resource/show_edit', local_assigns %>
<% else %>
  <%= render 'cms/agents/addons/opendata_ref/resource/show_show', local_assigns %>
<% end %>
