<% value ||= nil %>
<% file = value.try(:file) %>
<%= render 'cms/agents/columns/main/base', show: true, column: column, value: value do %>
  <% if file.present? %>
    <div id="selected-files">
      <div class="file-view">
        <%= link_to file.url, class: :thumb, target: "_blank" do %>
          <% if file.image? %>
            <%= image_tag file.thumb_url, alt: file.name %>
          <% else %>
            <span class="ext icon-<%= file.extname %>"><%= file.extname %></span>
          <% end %>
        <% end %>

        <div class="name">
          <label>
            <%= file.name %>
          </label><br />
        </div>
        <div>
          <% case column.file_type %>
          <% when "image" %>
            <label><%= t("cms.column_file_upload.image.label") %>: <%= value.try(:label) %></label><br />
            <label><%= I18n.t("cms.options.column_image_html_type.#{value.try(:image_html_type)}") %></label>
          <% when "attachment" %>
            <label><%= t("cms.column_file_upload.attachment.label") %>: <%= value.try(:label) %></label>
          <% when "video" %>
            <label><%= t("cms.column_file_upload.video.text") %>: <%= value.try(:text) %></label>
          <% when "banner" %>
            <label><%= t("cms.column_file_upload.banner.link") %>: <%= value.try(:link) %></label>
            <label><%= t("cms.column_file_upload.banner.label") %>: <%= value.try(:label) %></label>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
