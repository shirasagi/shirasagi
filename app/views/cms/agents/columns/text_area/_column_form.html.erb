<% value ||= nil %>
<% controller ||= false %>
<% name = "#{f.object_name}[][in_wrap][value]" %>

<%= render 'cms/agents/columns/main/base', f: f, column: column, value: value, controller: controller do |id| %>
  <%= text_area_tag(name, value.try(:value), column.form_options.merge(id: nil, data: { syntax_check: "value", syntax_check_name: column.name })) %>
<% end %>
