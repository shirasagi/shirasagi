<% addons.each do |addon| %>
  <% buf = nil %>
  <% buf = render file: addon.show_file if addon.show_file %>
  <% next if buf.blank? %>
  <div class="addon-view" id="addon-<%= addon.id %>">
    <header><h2><%= addon.name %></h2></header>
    <div class="addon-body">
      <%= buf %>
    </div>
  </div>
<% end %>
