<nav class="nav-menu">
  <%=
    source = CGI.unescape(params[:source])
    if UrlValidator.valid?(source, absolute_path: true)
      link_to t('ss.links.back'), source
    end
  %>
  <%= link_to t("history.restore"), action: :restore if @item.restorable? %>
</nav>
