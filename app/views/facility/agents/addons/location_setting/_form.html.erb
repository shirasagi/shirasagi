<dl class="see mod-facility-location">
  <dt><%= t("facility.location.setting") %><%= @model.tt :st_locations %></dt>
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "st_location_ids[]", value: "", class: "hidden-ids" %>
    <%= link_to t("facility.location.index"), facility_apis_locations_path, class: "ajax-box" %>
  </dd>
  <dd>
    <table class="index ajax-selected">
      <thead>
        <tr>
          <th class="name"><%= t("facility.location.name") %></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @item.st_locations.each do |cate| %>
        <tr data-id="<%= cate.id %>">
          <td><%= f.hidden_field "st_location_ids[]", value: cate.id %> <%= cate.name %></td>
          <td><%= link_to t("cms.apis.categories.deselect"), "#", class: :deselect %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </dd>
</dl>
