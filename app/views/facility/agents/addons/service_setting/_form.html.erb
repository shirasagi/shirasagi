<dl class="see mod-facility-service">
  <dt><%= t("facility.service.setting") %><%= @model.tt :st_services %></dt>
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "st_service_ids[]", value: "", class: "hidden-ids" %>
    <%= link_to t("facility.service.index"), facility_search_services_path, class: "ajax-box" %>
  </dd>
  <dd>
    <table class="index ajax-selected">
      <thead>
        <tr>
          <th class="name"><%= t("facility.service.name") %></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @item.st_services.each do |cate| %>
        <tr data-id="<%= cate.id %>">
          <td><%= f.hidden_field "st_service_ids[]", value: cate.id %> <%= cate.name %></td>
          <td><%= link_to t("cms.search_categories.deselect"), "#", class: :deselect %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </dd>
</dl>
