<% @tap_menu = proc do |item| %>
  <%= link_to t('ss.links.show'), cms_member_path(id: item) if item.allowed?(:read, @cur_user, site: @cur_site) %>
<% end %>

<% @index_title = proc do |item| %>
  <%= link_to item.name || item.id, cms_member_path(id: item), class: "title" %>
<% end %>

<% @index_meta = proc do |item| %>
  <span class="id">#<%= item.id %></span>
  <span class="datetime"><%= ss_time_tag item.updated %></span>
  <span class="email"><%= item.email %></span>
<% end %>

<%= render template: "cms/crud/index" %>
