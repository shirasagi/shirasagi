<dl class="see mod-chat-category">
  <dt><%= @model.t :category_ids %><%= @model.tt :category_ids %></dt>
  <dd>
    <%= f.hidden_field 'category_ids[]', value: '', id: nil, class: 'hidden-ids' %>
    <%= link_to t('cms.apis.categories.index'),
                chat_apis_categories_path(cid: @cur_node.id), class: 'ajax-box' %>
  </dd>
  <dd>
    <table class="index ajax-selected">
      <thead><tr><th class="name"><%= Chat::Category.t :name %></th><th class="deselect"></th></tr></thead>
      <tbody>
        <% @item.categories.each do |item| %>
        <tr data-id="<%= item.id %>">
          <td><%= f.hidden_field 'category_ids[]', value: item.id, id: nil %> <%= item.name %></td>
          <td><%= link_to t('ss.buttons.delete'), '#', class: 'deselect btn' %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </dd>
</dl>
