<dl class="see mod-chat-category">
  <dt><%= @model.t :category_ids %></dt>
  <dd>
    <% @item.categories.each do |category| %>
      <% next unless category %>
      <span class="chat-category-label"><%= category.name %></span>
    <% end %>
  </dd>
</dl>
