<% message.subject = mailer.render_title %>
<% if @action.publishing_office_state == 'show' %>
<%= mailer.control_datetime_format %>　<%= mailer.publishing_offices.join("・") %><%= mailer.publishing_offices.length > 1 ? "　共同発表" : "発表" %>

<% end %>
<%= render file: "_create" %>
<%= @action.signature_text %>
