<% return if @item.send(:html).blank? %>

<%= html_editor "#item_html", height: "400px", readonly: true %>

<dl class="see member-body">
  <dd class="wide">
    <%= text_area_tag "html", sanitize(@item.html), id: :item_html, style: "height: 400px;" %>
  </dd>
</dl>
