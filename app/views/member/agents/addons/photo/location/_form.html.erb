<dl class="see mod-location">
  <dt><%= @model.t :photo_locations %><%= @model.tt :photo_locations %></dt>
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "photo_location_ids[]", value: "" %>
    <% if @locations.present? %>
      <% @locations.each do |loc| %>
        <label>
          <%= f.check_box("photo_location_ids[]", {}, loc.id) %>
          <%= loc.name %>
        </label>
      <% end %>
    <% end %>
  </dd>
</dl>
