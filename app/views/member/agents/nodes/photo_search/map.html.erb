<%= render partial: "upper" %>

<%= render_map "#map-canvas", markers: @markers, site: @cur_site, zoom_level: @cur_page.map_zoom_level %>

<div id="map-canvas" style="width: 100%; height: 400px;"></div>

<%= paginate @items if @items.try(:current_page) %>
