<%

def search_node_options
  Faq::Node::Search.site(@cur_site).map do |node|
    [node.name, node.id]
  end
end

%>
<dl class="see mod-faq-search">
  <dt><%= @model.t :search_node %><%= @model.tt :search_node %></dt>
  <dd><%= f.select :search_node_id, search_node_options, include_blank: true %></dd>
</dl>
