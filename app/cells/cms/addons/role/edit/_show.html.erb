<dl class="see">
  <dt><%= @model.t :role_ids %></dt>
  <dd>
    <% @item.roles.site(@cur_site).order_by(name: 1).each do |role| %>
    <div><%= link_to role.name, cms_role_path(id: role) %>
    <% end %>
  </dd>
</dl>
