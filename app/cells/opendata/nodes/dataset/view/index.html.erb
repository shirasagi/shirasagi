<%= jquery do %>
SS_Tabs.render("#opendata-tabs-<%= @cur_node.id %>");
<% end %>

<div class="dataset-count">
  登録データ件数 <span><%= @count %></span>件
</div>

<div class="opendata-tabs tabs" id="opendata-tabs-<%= @cur_node.id %>">
  <% @tabs.each_with_index do |tab, idx| %>
  <article class="tab" id="cms-tab-<%= @cur_node.id %>-<%= idx %>">
    <header><h1><%= tab[:name] %></h1></header>
    <div class="pages">
      <% tab[:pages].each do |item| %>
      <article>
        <header>
          <time datetime="<%= item.date.to_date %>"><%= I18n.l item.date.to_date, format: :long %></time>
          <h2><%= link_to item.name, item.url %></h2>
        </header>
      </article>
      <% end %>
    </div>
    <nav class="feed">
      <%= link_to :more, tab[:url], class: "more" if tab[:url] %>
      <%= link_to "RSS", tab[:rss], class: "rss" if tab[:rss] %>
    </nav>
  </article>
  <% end %>
</div>

<article class="areas nodes">
  <header><h1>地域</h1></header>
  <div>
    <% @areas.each do |item| %>
    <article>
      <header>
        <h2><a href="<%= "#{@search_url}?s[area_id]=#{item.id}" %>">
          <span class="name"><%= item.name %></span>
          <span class="count">(<%= item[:count] %>)</span></a></h2>
      </header>
    </article>
    <% end %>
  </div>
  <nav>
    <%= link_to "もっと見る", @search_url, class: "more" %>
  </nav>
</article>

<article class="tags nodes">
  <header><h1>タグ</h1></header>
  <div>
    <% @tags.each do |data| %>
    <article>
      <header>
        <h2><a href="<%= "#{@search_url}?s[tag]=#{data['id']}" %>">
          <span class="name"><%= data["id"] %></span>
          <span class="count">(<%= data["count"] %>)</span></a></h2>
      </header>
    </article>
    <% end %>
  </div>
  <nav>
    <%= link_to "もっと見る", @search_url, class: "more" %>
  </nav>
</article>

<article class="formats nodes">
  <header><h1>フォーマット</h1></header>
  <div>
    <% @formats.each do |data| %>
    <article>
      <header>
        <h2><a href="<%= "#{@search_url}?s[format]=#{data['id']}" %>">
          <span class="name"><%= data["id"] %></span>
          <span class="count">(<%= data["count"] %>)</span></a></h2>
      </header>
    </article>
    <% end %>
  </div>
  <nav>
    <%= link_to "もっと見る", @search_url, class: "more" %>
  </nav>
</article>

<article class="licenses nodes">
  <header><h1>ライセンス</h1></header>
  <div>
    <% @licenses.each do |data| %>
    <article>
      <header>
        <h2><a href="<%= "#{@search_url}?s[license]=#{data['id']}" %>">
          <span class="name"><%= data["id"] %></span>
          <span class="count">(<%= data["count"] %>)</span></a></h2>
      </header>
    </article>
    <% end %>
  </div>
  <nav>
    <%= link_to "もっと見る", @search_url, class: "more" %>
  </nav>
</article>
