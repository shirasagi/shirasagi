<%= form_for :item, url: @resource_url, html: { id: "item-form", multipart: true } do |f| %>
<%= error_messages_for :item %>

<%= render file: '_form', locals: { f: f } %>

<footer class="send">
  <%= f.submit t(:save), class: :save %>
</footer>

<% end %>
