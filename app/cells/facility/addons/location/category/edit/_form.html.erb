<dl class="see mod-location-category">
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "location_category_ids[]", value: "" %>
    <% @cur_node.st_location_categories.each do |cate| %>
    <label>
      <%= f.check_box("location_category_ids[]", {}, cate._id) %>
      <%= cate.name %>
    </label>
    <% end %>
  </dd>
</dl>
