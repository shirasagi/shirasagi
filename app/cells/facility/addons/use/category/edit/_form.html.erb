<dl class="see mod-use-category">
  <dd class="wide" style="line-height: 2.5">
    <%= f.hidden_field "use_category_ids[]", value: "" %>
    <% @cur_node.st_use_categories.each do |cate| %>
    <label>
      <%= f.check_box("use_category_ids[]", {}, cate._id) %>
      <%= cate.name %>
    </label>
    <% end %>
  </dd>
</dl>
