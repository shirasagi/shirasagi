<%= tag.span **dialog_options do %>
  <%=
    if show_button?
      button_tag content || name, **button_options
    end
  %>
  <%= tag.template data: { "ss--dialog-target" => "content" } do %>
    <% if template? %>
      <%= template %>
    <% else %>
      <turbo-frame src="<%= url %>" id="ss-dialog-frame">
        <%= helpers.loading %>
      </turbo-frame>
    <% end %>
  <% end %>
<% end %>
