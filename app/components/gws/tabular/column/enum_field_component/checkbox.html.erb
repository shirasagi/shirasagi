<%= render Gws::Tabular::Column::BaseComponent.new(cur_site: cur_site, cur_user: cur_user, type: type, column: column) do %>
  <% select_options.each do |select_option| %>
    <%
      select_option = select_option.strip
      next if select_option.blank?

      select_option_translations = Gws::Column.to_translations(select_option)
    %>

    <label>
      <%= check_box_tag "#{form.object_name}[col_#{column.id}][]", select_option, normalized_values.include?(select_option), id: nil %>
      <%= select_option_translations[I18n.locale] || select_option_translations[I18n.default_locale] %>
    </label>
  <% end %>
<% end %>
